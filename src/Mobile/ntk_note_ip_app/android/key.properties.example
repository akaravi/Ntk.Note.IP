# Copy to android/key.properties (never commit). Used by app/build.gradle.kts release signing.
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=upload
storeFile=../upload-keystore.jks
